# Map files (large, mount via volume)
maps/*.bsp
maps/*.nav
maps/*.res
!maps/.gitkeep

# Compiled plugins (built in container)
*.amxx

# Sound files (copyrighted, not redistributable)
sound/quake/*.wav

# Environment variables
.env
